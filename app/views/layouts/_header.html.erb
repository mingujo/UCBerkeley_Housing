<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "UCB Family Housing", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <% if signed_in? %>
            <li id="fat-menu" class="dropdown">
              <%= link_to "Sign out", '/auth/logout', {:style=>'color:#FFD700;'}%>
            </li>
          <% else %>
            <li id="fat-menu" class="dropdown">
              <%= link_to "Sign in", '/auth/google_oauth2',  {:style=>'color:#FFD700;'}%>
            </li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
<h1>
  <%= link_to 'CALENDAR', events_path, :class => 'columnButton'%>
  <%= link_to 'CA INFO', cas_path, :class => 'columnButton'%>
  <%= link_to 'NEW SPREADSHEET', cas_generate_path, :class => 'columnButton'%>
</h1>
<br>
<br>